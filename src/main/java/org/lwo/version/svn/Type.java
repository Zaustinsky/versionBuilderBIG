package org.lwo.version.svn;

public enum Type {
    SQL_BEFORE,
    FORM,
    REPORT,
    TYPE_DOC,
    TYPE_ACC,
    PACKAGE,
    CONST,
    FUNCTION,
    ROLE,
    SQL_AFTER,
    JAVA_CLASS,
    UNKNOWN,
    JAR_FILE, // not svn
    MANUAL, // not svn,
    SPRAV //not svn
}
